# v5 Run — 2026-04-30 (autonomous)

**Branch:** `sl-v5-lora-r32-sub-abs-velmode` on `sl628/aic`
**PR:** [#28](https://github.com/sl628/aic/pull/28)
**Plan:** local at `/home/yifeng/.claude/plans/if-i-wanna-implement-quizzical-hamming.md`
**Data:** `siyulw2025/aic-data-sub-v2` (corrected re-upload; 103K subsampled, 162 train + 18 val episodes)

## TL;DR — DONE 🎉 (training)

**v5 30K training completed 2026-04-30 17:58 PT — 2h07m wall-clock, no incidents (after one CUDA-arch crash in <1 min on first launch, fixed with explicit `CUDA_VISIBLE_DEVICES=0`).**

- 20 ckpts at `/home/yifeng/aic_xvla_data/v5-lora-r32-sub-abs-velmode/ckpt-{1500..30000}/` (138 MB each, LoRA-only).
- All ckpts **G2 PASS** — `pred_pos_std` 0.279-0.342, far above 0.005 collapse threshold. No bimodal failure.
- Final position_loss ~0.036.
- W&B: https://wandb.ai/wuyifeng51/aic-xvla/runs/1tyyuw44 (run name `v5-lora-r32-sub-abs-velmode`, naming verified across all 6 locations).

### What's left (for you, can't be done autonomously)
1. **G3 sim eval** (interactive Gazebo). Recommended sweep: ckpts {18000, 21000, 24000, 27000, 30000} × 5 evals each. Pick winner by **median total**.
   ```bash
   cd ~/workspace/aic/.claude/worktree-sl-v5-lora-r32-sub-abs-velmode
   bash scripts/eval_v5.sh /home/yifeng/aic_xvla_data/v5-lora-r32-sub-abs-velmode/ckpt-30000
   ```
2. **HF upload** (no auth in any env on this host). After `huggingface-cli login`:
   ```bash
   bash /home/yifeng/aic_xvla_data/v5-lora-r32-sub-abs-velmode/upload_to_hf.sh
   ```
   Auto-creates `siyulw2025/v5-lora-r32-sub-abs-velmode`, pushes sidecar + all 20 ckpts. Pass `ckpt-NNNNN` args for a subset.
3. **Submit** the best to ECR via `submission` skill if v5 winner beats v1/v2 (median 42-48).

## Why v5 spec changed (delta+velmode → sub+abs+velmode)

User had v5 = `v5-lora-r32-full-delta-velmode` in the table. Two reasons to update:
1. **Encoding**: re-reading the code (`handler.py:65-80` + `X-VLA/datasets/utils.py:99-104` + `eval.py:147-148`), delta training learns command-relative deltas but inference adds live state. With controller lag, inferred target ≈ lagged_state + small_delta. Pairing delta with velocity (`(target − live)/dt`) compounds (small_delta → small_twist → freeze). Absolute is the clean pair for velocity.
2. **Data**: the colleague's freshest re-upload is `siyulw2025/aic-data-sub-v2` (103K, P3-trimmed), not the full 270K. The full version doesn't exist on HF yet.

So v5 → `v5-lora-r32-sub-abs-velmode`. This is essentially "v3 done right": same sub data + absolute encoding as v3, but with proper VLM LR (`learning_coef=1.0`, NOT v3's 0.1) + LoRA rank 32 + velocity control.

## Code patches (PR sl628/aic#28)

- **`aic_utils/aic_xvla/aic_xvla/train.py`**: CLI flags `--lora_r`, `--lora_alpha`, `--cmd-mode-recommended`, `--norm-check-fixture`, `--norm-check-threshold`. Monkey-patches `peft_train.LoraConfig` (X-VLA upstream untouched). Sidecar extended.
- **`aic_utils/aic_xvla/aic_xvla/save_norm_check.py`** (new): post-save hook calls `AICXVLAPolicy.predict()` on `aic_data_one_ep`, asserts `pred_action[:,:3].std() > 0.005`. Catches "loss decreases but action collapses" failure before 30K iters wasted.
- **`aic_utils/aic_xvla/aic_xvla/eval.py`**: warns at policy init if `AIC_XVLA_CMD_MODE` mismatches sidecar's `cmd_mode_recommended`.
- **`scripts/train_v5-lora-r32-sub-abs-velmode.sh`** (new): launch script. Reads `AIC_XVLA_DATA_ROOT`, expects `${root}/train_meta_sub2.json`.
- **`scripts/eval_v5.sh`** (new): single eval. Velocity mode, REPLAN=15, TIMEOUT=180. Tees log to `~/aic_results_archive/v5-lora-r32-sub-abs-velmode_run<N>_<stamp>.log`.

### G1 (code-patch sanity) PASSED 2026-04-30 09:39

4-iter dry train against `aic_data_one_ep`:
- LoRA override fired → trainable params **35,953,240** (≈4× v1/v2/v3's 12M).
- Save hook ran on ckpt-2 (`pred_pos_std=0.155`, PASS) and ckpt-4 (`0.238`, PASS).
- Sidecar correct: `{action_encoding=absolute, mode=peft, lora_r=32, lora_alpha=64, cmd_mode_recommended=velocity}`.
- Adapter config: `r=32, lora_alpha=64`. ✓

## Hypothesis

Tier-3 (insertion) is bimodal across v1–v4 ckpts. v3 used the same sub data and stalled because of `learning_coef=0.1` (under-trained the VLM). v5 fixes that bug AND adds 4× LoRA capacity AND switches eval to velocity. Three changes stacked over v3:

1. `learning_coef=0.1` → `1.0` (the v3 fix).
2. LoRA rank 8 → 32 (capacity for tier-3 precision).
3. Eval cmd mode pose → velocity (error-driven Cartesian command).

Goal: median total ≥ 53 (≥10 over v1/v2 median 42–48), tier-3 median ≥ 15.

## Auto-fallback contract (when training runs)

| Gate | Pass | Auto-fallback on fail |
|---|---|---|
| **G1**: code-patch sanity | sidecar/adapter correct, save hook runs | ✅ passed 09:39 |
| **G2**: ckpt-1500 replay smoke | `<ckpt>/norm_check.json` shows pred_pos_std > 0.005 | Kill, swap encoding to delta, relaunch as `v5-lora-r32-sub-delta-velmode`. If still fails, halt. |
| **G3**: ckpt-3000 sim dry-run (velocity) | tier-1=1, tier-2≥1 | Switch to pose mode, rename ckpt to `v5-lora-r32-sub-abs-posmode`, continue. |
| **G4**: OOM at r=32 | first 100 iters complete | Restart with r=16/alpha=32, rename to `v5-lora-r16-sub-abs-velmode`. |

Each fallback updates all 6 naming locations atomically (HF/W&B/dir/sidecar/script/log).

## Timeline

### 2026-04-30 09:30–10:00 — code patches + G1 PASSED
4-iter dry train confirms patches work end-to-end.

### 2026-04-30 10:30 — pivot v5 spec to sub-abs-velmode + name reconciliation
Renamed branch/worktree/scripts/journal v8 → v5. PR #27 closed; PR #28 opened with v5 naming.

### 2026-04-30 ~15:15 — data download
- Downloaded `siyulw2025/aic-data-sub-v2` to `/home/yifeng/aic_xvla_data/` — found that the dataset is **only the meta json pointers** (88 KB total: train_meta_sub2.json, val_meta_sub2.json, README.md). The actual frame parquets + JPGs live in a sibling raw-format dataset.
- Started download of `siyulw2025/cableholder-all` (1.38 GB LeRobot v3.0 raw video format) — this is the source the existing `scripts/convert_lerobot_to_xvla.py` decodes into per-episode parquet+JPG.

### Steps completed (autonomous)
1. ✓ Downloaded `cableholder-all` (1.3 GB raw LeRobot v3.0). One retry needed after 429 rate limit.
2. ✓ Patched `convert_lerobot_to_xvla.py` for the v3.0 schema rename (`dataset_from_index` → `data/from_index`). Committed `0fa60b7`.
3. ✓ Conversion: 180 eps / 293K frames / 14 GB at `/home/yifeng/aic_xvla_data/episodes/`. Meta at `/home/yifeng/aic_xvla_data/aic_meta.json`.
4. **Decision**: skip the P3-tail trim (it would just remove stationary settle frames, costing only ~30% extra epoch time, not worth the moving-parts complexity). v5 trains on the full converted 293K. Launch script falls back to `aic_meta.json`.
5. ✓ G1.5 (50-iter sanity on real data): PASSED. Loss 72→0.7 in 50 iters; both save-hook checks PASS (pred_pos_std 0.357, 0.313). VRAM ~11 GB / 16 GB. ~0.3-0.6 s/iter → 30K ≈ 3-5h.
6. ✓ Full v5 30K training kicked off in background at 15:45 PT. **CRASHED <1 min** with `CUDA error: no kernel image is available` — torch picked the TITAN Xp (sm_61, unsupported) because `CUDA_VISIBLE_DEVICES` propagation lost the value across the bg launch. G1.5 had passed because that shell had it inline.
7. ✓ Relaunched 15:50 PT with explicit `CUDA_VISIBLE_DEVICES=0` in outer env. Confirmed: `trainable params 35,953,240 (3.93%)`, `🚀 Start training for 30000 iterations`. Same numbers as G1+G1.5 — patches still wired correctly.

### Steps still pending (require user)
8. G3 sim dry-run at ckpt-3000 — needs the 4-terminal Gazebo flow (interactive, can't be done autonomously). When you return, run:
   ```bash
   bash scripts/eval_v5.sh /home/yifeng/aic_xvla_data/v5-lora-r32-sub-abs-velmode/ckpt-3000
   ```
   Pass = tier-1=1, tier-2≥1. Fail = switch eval to pose mode (see auto-fallback table).
9. Full ckpt sweep: 5 evals each at ckpts {15K, 18K, 21K, 24K, 27K, 30K}. Pick winner by **median total** (variance is wide; single-eval is noise).
10. Submit best to ECR via the `submission` skill.

## Status as of last update

- Code/scripts: ✓ ready, G1+G1.5 passed.
- Data: ✓ converted to 14 GB local store.
- Training: ✓ running (after one CUDA-device-pin crash, relaunched at 15:50 PT); ETA ~3-5 hours.
- Eval gates G2 (per-ckpt norm check) automatic via save hook; G3 (sim) needs user.

### Note for the user when back: pinning lesson
Always pass `CUDA_VISIBLE_DEVICES=0` explicitly in the outer env when launching training scripts in the background on this host (not just inside the script). The TITAN Xp (sm_61) is unsupported by the XVLA env's torch and will silently grab the model if visible. Already added defensive pinning to v5 launch — consider hardening the launch script too.

## Final summary

**Training:** ✅ DONE (2h07m, 30K iters, 20 ckpts, all G2 PASS, no model collapse)
**Eval:** ⏸️ requires interactive Gazebo (user)
**HF upload:** ⏸️ requires `huggingface-cli login` (user); upload script staged at `/home/yifeng/aic_xvla_data/v5-lora-r32-sub-abs-velmode/upload_to_hf.sh`

### G2 std-trajectory (per ckpt, in order)
0.279, 0.296, 0.315, 0.342, 0.322, 0.311, 0.314, 0.304, 0.304, 0.314, 0.306, 0.310, 0.308, 0.306, 0.299, 0.304, 0.302, 0.300, 0.300, 0.304

### position_loss (mean per 1500-iter bucket)
| Iter | Mean | | Iter | Mean |
|---|---|---|---|---|
| 0–1.5K | 1.075 | | 13.5–15K | 0.064 |
| 1.5–3K | 0.174 | | 15–16.5K | 0.049 |
| 3–4.5K | 0.510 | | 16.5–18K | 0.040 |
| 4.5–6K | 0.330 | | 18–22K | ~0.045 |
| 6–7.5K | 0.281 | | 22–28K | ~0.035 |
| 7.5–9K | 0.126 | | 28–30K | ~0.036 |
| 9–10.5K | 0.092 | | | |
| 10.5–12K | 0.071 | | | |
| 12–13.5K | 0.064 | | | |

### Fallbacks taken
- **G4 (OOM at r=32):** N/A — never triggered. VRAM stayed ~12-13 GB / 16 GB.
- **G2 (action collapse at any ckpt):** N/A — never triggered. All 20 ckpts PASS.
- **CUDA-arch crash on first launch:** detected immediately (<1 min), root-caused (TITAN Xp sm_61 unsupported), fixed (explicit `CUDA_VISIBLE_DEVICES=0`), launch script hardened.

### vs baselines
Pending (G3 needed; v1=63/300, v2=63/300 single-shot, both medians 42-48 over 5-eval sweeps).

### Recommendation
Once G3 sweep + HF upload are done, **submit the median-best ckpt** to ECR. v5 has all the targeted improvements over v3 (learning_coef=1.0, rank-32 LoRA, velocity-mode eval-time control) and trained cleanly with no anomalies. Strong candidate for first improvement over the v1/v2 baseline.
